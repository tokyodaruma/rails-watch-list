<% @bookmarks.each do |bookmark| %>
  <%= render 'shared/showcard', bookmark: bookmark %>
<% end %>

<%= link_to 'Create new movie', new_list_bookmark_path(@list) %>
<%= link_to 'Back to the top', lists_path %>
